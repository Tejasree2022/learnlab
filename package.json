{
  "name": "learnlab",
  "version": "1.0.0",
  "description": "Learn anything with explanations and practice tasks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup-db": "node -e \"require('dotenv').config(); const {Client} = require('pg'); const client = new Client(); (async () => { await client.connect(); await client.query('CREATE DATABASE learnlab'); console.log('✅ Database created'); await client.end(); })()\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
